<div class="container">
  <p id="notice"><%= notice %></p>

  <div class="col-lg-12 col-md-12 col-xs-12 h3 text-center roboto">Welcome to TourPack</div>

  <%#= react_component('TourPack', 
                      {:presenter => @tour_packages.to_json}) %>
  <% if current_user.is_admin? %>
    <div class="col-lg-12">
      <%= link_to "<i class='fa fa-plus'></i> Create Tour Package".html_safe, new_tour_package_path, :remote => true, :class => "btn btn-primary pull-right","data-toggle" => "modal", "data-target" => "#tourPack" %>
    </div>
  <% end %>
  <div class="col-lg-12 col-md-12 flexContainer">
    <% @tour_packages.each do |tour_package| %>
      <div class="tourPack">
        <% if current_user.is_admin? %>
          <div class="pull-right">
            <%= link_to "<i class='fa fa-pencil'></i>".html_safe, edit_tour_package_path(tour_package), :remote => true %>
            <%= link_to "<i class='fa fa-close'></i>".html_safe, tour_package, method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
        <% end %>
        <div class="title text-center h4"><%= tour_package.name.capitalize %></div>
        <div><%= tour_package.age_from %></div>
        <div><%= tour_package.age_upto %></div>
        <div><%= tour_package.max_people %></div>
        <div><%= tour_package.cost_per_person %></div>
        <div><%= tour_package.last_submission_date %></div>
        <div class="text-center"><%= link_to 'View Plan', tour_package, :class => "btn btn-success" %></div>
      </div>
    <% end %>
  </div>
  <div class="text-center">
    <%= will_paginate @tour_packages %>
  </div>

  <div id="tourPack" class="modal fade" role="dialog">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">New Tour Pack</h4>
        </div>
        <div class="modal-body">
        </div>
      </div>

    </div>
  </div>
</div>